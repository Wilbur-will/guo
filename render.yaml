services:
  - type: web
    name: guo-chuang
    runtime: python
    rootDir: my-app
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
